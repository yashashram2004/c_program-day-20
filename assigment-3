#include <stdio.h>

int main() {
    int n, i;
    float sum = 0.0;

    printf("Enter a integer n: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        sum += 1.0 / i;
    }

    printf("Sum of the expression: %.4f\n", sum);

    return 0;
}
